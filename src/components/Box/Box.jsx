import styled from 'styled-components'
import { color, space, layout, border, shadow, typography, flexbox } from 'styled-system'

const Box = styled('div')(
    color,
    space,
    layout,
    border,
    shadow,
    typography,
    flexbox,
)

export default Box